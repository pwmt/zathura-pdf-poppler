zathura-pdf-poppler
===================
The zathura-pdf-poppler plugin adds PDF support to libzathura by using the
poppler rendering library.

Requirements
------------
libzathura (>= 0.0.1)
poppler-glib
check (>= 0.9.8) (for tests)
libfiu (>= 0.91) (for fault-injection)
python-docutils (for man pages)

Installation
------------
To build and install the plugin:

  make install

Uninstall:
----------
To delete the plugin from your system, just type:

  make uninstall
